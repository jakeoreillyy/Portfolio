// Ghost accent button that fills in on hover: the nav contact pill and the
// project card repo links. Radius matches the Tag pill (rounded, 4px) so every
// bordered control on the site shares one corner.
export const accentButton =
  "inline-flex items-center gap-1.5 rounded border border-accent/40 px-3.5 py-1.5 " +
  "font-mono text-[13px] text-accent transition-colors " +
  "hover:border-accent hover:bg-accent hover:text-background";
